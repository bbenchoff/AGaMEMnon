# FPGA parity ledger

This is the public, generated summary of the AG32-Docs family-level feature ledger.
It distinguishes recovered encodings, open-flow implementation, and silicon
qualification. A companion machine manifest covers all 136 parameter
declarations on the six primitive families actually present on AGRV2K. It
separates declarations, candidate domains, backend acceptance, open support,
and behavior rather than treating Verilog widths as legality evidence.
The generated [D0 claim-policy ledger](CLAIM_POLICY_LEDGER.md) is the companion
permission ledger: it records maturity and evidence tier independently and
does not widen any feature claim in this parity table. Both ledgers are
**fabric-side only**. Neither lists MCU hard peripherals (UART, SPI, I2C, CAN,
flash, USB, DMA, CRC, watchdog, MTIME, RTC) or the analog blocks, so the absence
of a row here is not a negative claim about them — those live in
[STATUS.md](STATUS.md) and [HARDWARE_VALIDATION.md](HARDWARE_VALIDATION.md).

**v0.4.0 current overlay:** the reconciled corpus has 74 bounded successes,
2 correctness escapes and 14 no-image classifications; 15 other cases have
reference/harness limitations. Initialized x1/x18 single-port ROM is admitted
in the characterized X13Y4/L48 read-only mode, not general writable/dual-port
memory. See [release scope](RELEASE_0_4_0.md); dated generated rows below retain
their historical evidence scope.

**2026-08-24 historical campaign overlay:** this generated family ledger predates the
composition campaign unless a row explicitly says otherwise. The campaign's
105 hand-authored designs include 52 routability gaps and 13 correctness
escapes. Exact row-level encoding or prior path qualification never generalizes
to arbitrary state machines, BRAM output routes, physical inputs, clock reach,
or density. [STATUS.md](STATUS.md) is the current release authority.

The historical scalar witness remains bounded: **PIN_12 is qualified only as a scalar single-consumer direct combinational input** in that exact
composition. Later held-input recovery has its own controls and scope; neither
witness qualifies arbitrary input routes.

| Feature | Encoding | Open flow | Silicon | Packages | Current boundary |
|---|---|---|---|---|---|
| 164-byte global/configuration-chain preamble | complete | supported | qualified_subset | L48 | Idle plus seven byte-exact SYSCLK/HSE profiles are generated declaratively and replace inherited preamble bytes. For HSE=8 the silicon-measured set is now *wider* than the byte-exact profile set (43 rates, 4-248 MHz); for HSE=16 and HSE=12 there is no silicon evidence at all, because the reference board has an 8 MHz HSE. |
| Non-preamble tile-grid reset/default canvas | complete | supported | qualified_subset | L48 | The whole design-neutral default canvas is synthesized from scratch by default (promoted decoded tables; no canvas byte copied) and configures on L48 silicon; fabric_default.bin is retained only as a non-loadable (stale-CRC) decode reference. The function of the unnamed reserved selector bit-lines and 15 XXXX spares is unproven (position and reset value only). |
| LUT4 and flip-flop RTL | complete | supported | qualified_subset | L48 | Exact small combinational, counter, shift, LFSR, and state profiles are qualified. Feedback, FSM, rotate/reset, add/reset, far-site state, and dense-state campaign vehicles have open correctness escapes; arbitrary RTL/state is not qualified. |
| General fabric routing selectors | partial | partial | qualified_subset | L48 | Conflict-free physical and unanimous relative selectors are fail-closed; the negative-evidence mechanism remains, but its historical set is empty. Current production count: 14 of 14 admitted; 0 conservatively blocked as unverified. Eight direct clock-free pad-to-pad witnesses now qualify `RMUX26@15,4->RMUX09@14,4`, `RMUX33@15,4->RMUX39@14,4`, `RMUX80@15,7->RMUX33@15,4`, `RMUX21@14,8->RMUX87@14,5`, `RMUX21@14,9->RMUX87@14,7`, `RMUX69@14,6->RMUX76@14,10`, `RMUX09@14,4->RMUX28@14,8`, and `RMUX15@3,4->RMUX68@6,4`. The closing PIN_25-to-PIN_18 sibling/target made its selected edge the only non-clock x=3.5 crossing, shared the compact suffix into fixed X6Y4_SLICE11.I[3] and the complete output route, carried zero selector debt or mux conflict, FCB-accepted, and reproduced the same dual-pull inverse truth table. Static results from constrained constructions remain uninterpretable when matched sibling controls also fail. Evidence: `qualification/conduction_ungate_evidence.jsonl`. |
| Global clock distribution | partial | partial | qualified_subset | L48 | One global spine (GCLK0; a single spine is exposed by default) at the fixed qualified seam selector 5, with per-tile clock-selection data covering all 132 LogicTiles and one qualified hard-HSE input-enable bit. The silicon evidence is one isolated distribution oracle, not a survey: GCLK0 into X12Y3_ClkMUX02 driving a toggle flip-flop, read out through a separate registered transport (routing_evidence.jsonl, 2026-07-13-x12y3-clkmux02-isolated), alongside the tiles exercised incidentally by other qualified designs. The former phrase 'near and far logic tiles' had no coordinates behind it and is withdrawn as unfalsifiable. Additional spines, other seam or tile clock selectors, derived or gated clocks, clock enables, and skew are unsupported; clock SOURCE selection is not a fabric bit at all, since the top GCLKSW mux is selected by an MCU runtime register. |
| PLL configuration | partial | partial | qualified_subset | L48 | Emission is one closed-form divider equation, differentially validated byte-exact on 53/53 vendor sweep preambles, fail-closed to silicon-qualified HSE=8 SYSCLK plus the seven byte-exact profiles. The silicon frequency-qualified surface is HSE=8, SYSCLK 4-248 MHz (43 rows, worst 0.058%). A separate five-site state vehicle returned zero despite correct routed logic (`VP-AGM-007`), so frequency evidence does not qualify arbitrary physical clock reach. No phase, duty, feedback, other-output, alternate-HSE, or broad distribution claim. |
| Internal/external oscillator modes | partial | absent | unqualified | L100, L64, L48, Q32 | Static recovery separates OSC pad-chain fields, dedicated crystal pads, virtual HSI/HSE/OSC sources, and MCU controls. The qualified clock path still uses HSE into fixed PLL profiles; no general oscillator source is implemented. Nothing is emitted: the oscillator pins are excluded from user IO by the device model, and no alta_osc site is declared on AGRV2K, so user RTL cannot instantiate one. The package list reflects decoded chain data only -- on Q32 the OSC chain site is NC_92, so there is no oscillator pad there to have a mode. |
| BRAM9K modes and routing | partial | partial | qualified_subset | L48 | Retained exact X13Y4 read/write subsets remain path-specific evidence. New initialized x1 and x18 Port-A compositions preserved the currently modeled INIT/config surface but read zero (`VP-AGM-006`); the affected `i1-d0` profiles now refuse. Vendor multi-site observations and route harvests remain reference-only. Ordinary/inferred writes, alternate output corridors, open-flow X13Y1..Y3 routes, wider multi-site output, independent clocks, collision semantics, broader dual-port operation, remaining modes, and most B4 rows remain unqualified. |
| Dedicated carry chains | partial | partial | qualified_subset | L48 | Same-tile short chains and one 33-site corridor with up to 32 arithmetic stages are opt-in and qualified, all in column X20. A 2026-08-15 vendor-corpus harvest reframes what limits that: all 113 geometrically possible inter-tile carry seams appear in af.exe placements, spanning every one of the 19 LogicTile columns (19,790 crossings over 3,842 routed netlists), and seam-crossing slices carry the same per-slice pattern as any other stage. Whether the inter-tile CONNECTION itself needs configuration is now ANSWERED for one seam, and the answer is no: a downward crossing at X15Y2_CARRYOUT15->X15Y1_CARRYIN00 counts correctly on silicon (distinct=4, both lanes varying) in an image where the emitter contributed zero bits for that hop, so the crossing is dedicated hardware selected by PLACEMENT, not a routed mesh net needing config. That also removes the reason to doubt the recorded X20 33-stage pass. Two invariants hold with zero exceptions: every crossing runs (x,y)->(x,y-1) via SLICE15->SLICE0, and the vendor never crosses out of a partially filled tile. Observation is not conduction: the silicon-qualified surface is unchanged. |
| Package pin/bank legality | complete | supported | qualified_subset | L100, L64, L48, Q32 | Distinct recovered bond maps exist for all packages; only L48 is silicon-qualified and other packages warn explicitly. PIN_n is the decimal physical package-lead label used on the board and in the L48 bond map: PIN_10 through PIN_19 mean leads 10 through 19, not hexadecimal indices 0x10 through 0x19. |
| Physical input routing | partial | partial | qualified_subset | L48 | Retained exact demonstrations for PIN_10/11/12/15/19 and four left-edge direct inversions remain qualified only as their exact compositions. Independent PIN_10/PIN_12 and SPI0/SPI1 ingress defects have controlled recovery and fresh-build evidence; known bad images remain rejected. See [SPI qualification](SPI_RECEIVE_QUALIFICATION.md). No generic input-route, fanout, registered-capture, pin-chain, or electrical claim follows from the older paths. |
| Physical output routing | partial | partial | qualified_subset | L48 | Silicon-qualified L48 outputs are the left-edge PIN_25 through PIN_28 header outputs and all ten decimal top-edge package leads PIN_10 through PIN_19. The labels are decimal physical package leads, not hexadecimal indices. Every top-edge image originally built through the ordinary CLI under the research-unsafe profile with zero unmapped, predicted, or legacy-absolute selectors and no raw-image post-processing; as of 2026-08-17 nine of the ten (all but PIN_15) plus the left-edge four also build release-strict through `--uarch --pcf`, each FCB-confirmed on real silicon, and the Pico toggle re-witness of that vehicle's own images has since closed for all ten (io_evidence.jsonl trial pad-uarch-pcf-toggle-rewitness-20260817), each toggling under both pulls on exactly its intended lead; five qualified-input demonstration builds remain config-accept-only on this vehicle after an honest-negative toggle re-witness attempt. The closing PIN_10/PIN_11 singles and same-tile pair toggle only GP4/GP1 under both Pico pulls; their production pair is byte-identical to the measured candidate and its retained route repacks byte-identically. PIN_25 additionally has exact combined-cell qualifications: constant-source A/B proves static input readback and OE polarity 0 release / 1 drive-low; a local self-toggle proves high-rate dynamic OE through one six-pip corridor; and the ordinary PCF production path proves stepped external PIN_10 control with simultaneous readback under both pulls through the fail-closed RMUX15->RMUX53->IMUX11 entry. A retained four-link vendor oracle separately silicon-qualifies release/drive-low and active-high OE polarity through all four distinct exact PIN_25..PIN_28 OE corridors. Ordinary control-source ingress and simultaneous readback for PIN_26..PIN_28, high-rate simultaneous readback, the divergent RMUX20 branch, active drive-high, generic registered OE, and all other pins/corridors remain unqualified. Qualification remains exact L48 compositions, not arbitrary alternate routes or other packages. The historical nine-pad negative is void because those images contained no toggling signal. |
| MCU External-AHB slave boundary | partial | partial | qualified_subset | L48 | Silicon-qualified L48 subset: all 32 HRDATA corridors plus HREADYOUT/HRESP selector recovery; retained narrower public banks; one-hot local-interrupt commands for causes 16–19; and one exact composed 16-byte public map with canonical ID32=0x4147414d at +0 and zero-extended scratch16/counter3/W1C1 at +4/+8/+c. Three sequential full-map SRAM-only runs returned FCB 0x000f0002, nine zero error groups, counter seen mask 0xff, eight scratch observations, and reset-final [0x4147414d,0,0,0]. A distinct exact GPIO5-W1C derivative removes the bit1 self-test set hook and uses MCU GPIO5 DATA0/OUT_EN0 as an independently routed sustained-level source. The unchanged-base negative returned status signature 162, the dual-source OR control returned 2, and three production runs returned 0 while every non-status group remained zero. This proves low/hold/clear, high/set-priority and reset dominance for that software-controlled hard-boundary source; it is not a package-pin input, autonomous peripheral event, edge detector, asynchronous interrupt, or generic STATUS_SET owner. A second selectable exact derivative uses the synchronous fabric counter to emit one reset-rearmed count-seven event. Its unchanged negative, OR control, and three production runs produced causal signatures 0x04, 0x15, and 0x11 while every other public32 group remained zero, proving one autonomous HCLK-synchronous source. The release status-overlay path additionally admits one ordinary-Verilog, separately routed pure-fabric scalar status_set while preserving the core hash and rejecting extra ports, hard cells, incomplete routes, invalid clock roots, or BEL/wire conflicts. The unchanged base, route-identical zero-source control, and three live runs produced 0x04, 0x00, and 0x01 with zero non-status errors and zero strict-pack selector debt. This remains one exact four-word L48 composition, not a generic register-bank generator: signed or misaligned loads, higher/full-window decode, hardware non-SINGLE bursts, multi-bit or asynchronous/CDC status, interrupt wiring, shared-reset semantics, reservation-aware placement or guaranteed arbitrary-overlay fit, another package/device, hard MCU_RESETN, alternate bus clocks, generic direct-D lowering and broader interrupt state remain open. Non-SINGLE behavior remains an offline fail-closed protocol-core result; fabric-HRESP-to-MCU-fault is retired. |
| MCU local interrupt boundary and command state | complete | partial | qualified_subset | L48 | Four exact corridors independently deliver causes 16 through 19 with matching mip bits. Per-lane AHB write-command images qualify retained pending, mask/unmask, acknowledge, two re-arms, masked hold, and GPIO4.1 reset for every cause. One integrated strict image selects a one-hot cause with HWDATA[3:2] and uses HWDATA[1:0] for mask/ack/set commands. An SRAM-only MCU run counted three exact deliveries per cause, acknowledged each, re-armed twice, held events while masked, and cleared on GPIO reset. The integrated state is deliberately one shared pending/mask pair across the selected output, not four simultaneous pending stores. On the attached board, ordinary reset before the SRAM FCB load showed zero local mip with local mie clear and armed; configured held-reset/released state was zero. At the separately qualified default MCU bus clock (absolute rate an open question; MTIME measures 14.08 MHz), 64 sets and 64 acknowledgements each completed in exactly 21 MTIME ticks and synchronous reset clear took 40 ticks (tick counts, not a frequency). Reads return zero and no state-read claim is made. Note the narrower reading of the per-lane images: what is qualified is a *sequential one-hot* bank with one shared pending/mask pair, and an earlier per-lane simultaneous-pending topology failed and is retained as negative evidence in `mcu_ahb_local_int1_evidence.jsonl`. |
| MCU GPIO fabric boundary | partial | partial | qualified_subset | L48 | The four-bit GPIO4 inverter loopback is silicon-qualified. On L48, exact GPIO5 output-data/output-enable lanes 0 and 1 plus return input lane 2 are silicon-qualified through the pure open flow. The hard boundary requires terminal 8 on the seven inactive BBMUXS groups; zero-filled inactive terminals are not electrically safe. The qualification covers the two exact source pairs and one return lane only, not the full GPIO matrix or package pins. |
| IO electrical attributes | partial | partial | qualified_subset | L48 | Basic qualified input/output configurations are emitted for the exercised L48 pads, and per-pad electrical config is now **emitted and behaviourally witnessed**: 22 measured `CFG_PULL_UP` bit positions plus PIN_26 `CFG_OPEN_DRAIN` (`io_pad_electrical_L48.csv`), applied through the opt-in `AGAMEMNON_IO_PULLUP` / `AGAMEMNON_IO_OPEN_DRAIN` options in the preamble phase. On silicon, PIN_16 pull-up restores a released line to high 3/3 (control 0/3) and PIN_26 open-drain floats the high phase (TOGGLE under external pull-up, LOW 0/200 under pull-down, 3/3) — `qualification/fabric_io_electrical_evidence.jsonl`. The decoded device also supplies a checked 15-chain/four-alias/26-field static inventory and the complete 2–30 mA CFG_PDRCTRL mapping. Open: eleven special-function pad sites accept no `CFG_PULL_UP` through the vendor mechanism (emission fails closed there); no electrical parameter *values* are measured; and `CFG_KEEP` is **not** qualified — an undriven pad already retains its last driven level, so drive-and-release cannot discriminate a keeper (null result, recorded). The per-pad field layout is only partly known: the PULL_UP→OPEN_DRAIN spacing (+2) and the 6-bit inter-pad stride are confirmed by a passing prediction, but that stride cannot hold all decoded chain fields and `CFG_KEEP` does not follow it, so positions for drive/slew/input-enable must be measured, not derived. |
| Static timing and constraints | not_applicable | partial | qualified_subset | L48 | Conservative LUT, flip-flop, and carry cell arcs give fail-closed timing closure, plus a bounded exact overlay: a hash-pinned table of 542 certified local OMUX-to-IMUX pairs covering 9,375 of the 235,915 ordinary routing pips in the strict L48 graph, with the remaining 226,540 on the worst decoded delay per driving mux family. A requested but unavailable figure is fatal, never inferred, and the model is consistently pessimistic (historically 1.65x-4.1x), never optimistic. Timing closure is explicitly NOT a functional claim: timing_evidence.jsonl retains a nextpnr PASS at 100 MHz whose silicon run returned one static value across all 500 samples. |
| Project, source, and constraint compatibility | not_applicable | partial | software_only | L100, L64, L48, Q32 | The CLI supports ordinary synthesizable RTL, project inputs, PCF, and frequency targets for the documented open flow. |
| Vendor primitive and parameter compatibility | partial | partial | software_only | L100, L64, L48, Q32 | The 43-family multi-device library surface is explicitly joined to AGRV2K: four direct CELL_TYPEs, RIO through IO_TYPE, and the implicit core logic site are device-present; 37 library families are not declared on this device. |
| LZW, raw image, CRC, and AGASC handling | complete | supported | software_only | L100, L64, L48, Q32 | Canonical images round-trip byte-exactly; raw configuration, CRC, sparse named features, and LUT editing are supported. |
| Fully from-scratch configuration image | complete | supported | qualified_subset | L48 | The preamble, feature fields, and the whole 99,768-byte body are generated from scratch as the default base for every build, 100% byte-exact against the decoded canvas with no canvas byte copied. The generated image configures on L48 silicon (`FCB_STAT = 0x000f0002`) while the stale-CRC canvas is rejected (`0x00000040`), and designs built on either base are bit-identical across the retained pack corpus (evidence: `qualification/fabric_base_evidence.jsonl`). `fabric_default.bin` remains shipped only as a non-loadable (stale-CRC) decode reference, selectable via `AGAMEMNON_BASELINE`. Still open: the *function* of the unnamed reserved selector bit-lines and 15 `XXXX` spares is unproven — only their position and reset value are reproduced. |

The detailed workbench ledger also records vendor surfaces, concrete gaps, and
evidence paths. A feature marked `partial` must fail closed outside its documented
subset; package qualification does not transfer between packages.

The derived parameter ledger is
`agamemnon/chipdb/agrv2k_parameter_manifest.json`. Its BRAM width records
separate the six model candidates from the five direct modes accepted by
the open emitter; `legal_values` remains null pending stronger proof. The
RIO drive-current, pull-up, and open-drain domains are populated, but
their open support is empty and electrical behavior unqualified.
